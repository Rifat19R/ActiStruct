# Daily Log — 2026-06-29

## Goal

Phase 1 scaffold for the ActiStruct-nebwalk TMC reliability benchmark: env inspection, project architecture, pseudo manifest, reference schema stub, initial structures, QE relax inputs for the four primary neutral systems.

## Files changed

New project at `D:/Research/Dr.Kulik_MIT` (git-initialized, branch `feature/tmc-reliability-benchmark`): full directory tree, `configs/*.yaml`, `scripts/{_common,00..04,06}*.py`, `tests/*.py`, `references/reference_values_tmc_v0.yaml`, `structures/initial_xyz/*.xyz`, `qe/inputs/relax/*.in`, `qe/run_manifest_v0.csv`.

## Commands run

`python scripts/00_inspect_environment.py`, `01_bootstrap_project.py [--dry-run]`, `02_scan_pseudos.py`, `03_collect_reference_stub.py`, `04_build_initial_structures.py [--dry-run]`, `06_build_qe_inputs.py [--dry-run]`, `pytest tests/ -v`, plus a real `pw.x -input qe/inputs/relax/ni_co4_initial.in` smoke test via WSL (killed after 60s once SCF setup started cleanly).

## Tests passed

23/23 pytest tests across test_environment, test_pseudo_manifest, test_reference_schema, test_structure_generation, test_qe_input_builder.

## Results generated

- Confirmed `actistruct` (not `inverse_active`) is the real importable package, editable-installed from `D:/Rifat_kh/inverse_active` (v0.7.2), into the global Windows Python 3.14.4 environment — not into the repo's own (WSL-targeted) `.venv`.
- Confirmed `pw.x` v7.4.1 exists and runs at `/home/duets/q-e-qe-7.4.1/bin/pw.x` under WSL Ubuntu (user `duets`) — not on Windows PATH, not on WSL's default PATH either.
- Pseudo manifest: ready, all 6 required elements (Fe, C, H, Ni, O, Cr) found in `D:/Rifat_kh/SSSP_1.3.0_PBE_efficiency`.
- Reference schema: 4 entries, all `needs_manual_review`, zero fabricated values.
- 4 initial XYZ geometries built from symmetry + literature-typical bond lengths (explicitly labeled as guesses).
- 4 QE relax `.in` files generated; `ni_co4_initial.in` smoke-tested directly against the real `pw.x` binary — parsed cleanly, pseudopotentials loaded, SCF setup began with no errors.

## Scientific meaning

Phase 1 infrastructure (env -> pseudo -> reference schema -> structures -> QE inputs) is in place and traceable. No DFT relaxations have been run yet; no claims about converged geometries, energies, or ActiStruct/nebwalk performance are made at this stage.

## Bugs found and fixed

- `06_build_qe_inputs.py`: `prefix`/`outdir` initially used `complex_id` while the output filename and run-manifest `candidate_id` used `{complex_id}_initial`, breaking traceability between the `.in` file and its own internal prefix. Fixed by threading `candidate_id` through consistently.
- Built `scripts/05_generate_perturbation_candidates.py` (missing from the original phase 1 pass, per plan Sec 7.6). Refactored the script 04 geometry builders to accept optional perturbation kwargs (defaults unchanged, confirmed byte-identical baseline output before/after); 52 one-at-a-time perturbation candidates generated (12-16/complex), `tests/test_perturbation_candidates.py` added. 28/28 tests now pass.
- **OOM on the first real relax attempt:** Rifat ran `pw.x` on `ferrocene_initial.in` and hit `MPI_ABORT`. Two issues, fixed in sequence: (1) he'd used a nonexistent input path (`qe_inputs/initial_relax/ferrocene.in` vs. the real `qe/inputs/relax/ferrocene_initial.in`) - confirmed via QE's own `CRASH` file; (2) after fixing the path, the job ran but needed an estimated 54.79 GB total dynamical RAM against 16 GB WSL + 20 GB swap on a 32 GB host, and briefly destabilized the WSL VM. Root cause: `vacuum_padding_angstrom: 12.0`/side inflated the cell to ~28.3 A for a ~4.5 A molecule. Reduced to `6.0` (still standard for `assume_isolated='mt'`), regenerated all 4 `.in` files, re-verified ferrocene against the real `pw.x` binary - ran cleanly through several SCF iterations (energy -525.36 -> -524.93 Ry) at an estimated ~10.5 GB/process.
- **cr_co6 BFGS convergence failure (real data):** Rifat ran the sequential queue (ni_co4 -> cr_co6 -> fe_co5). ni_co4 and fe_co5 converged cleanly (`bfgs converged in 9/13 scf cycles`, final energies -583.5691160575/-629.6772928617 Ry). cr_co6 exited 0 but actually failed: `bfgs failed after 10 scf cycles and 7 bfgs steps, convergence not achieved` - exit code 0 does not mean converged, QE still prints `JOB DONE` on a failed-to-converge BFGS run. Traced the trajectory: `Total force` dropped to ~0.0004 Ry/au (close to `forc_conv_thr=1e-4`) but the trust radius collapsed below QE's default `trust_radius_min` floor and BFGS gave up. Root cause: `ibrav=0` (QE itself flagged this `DISCOURAGED`) used a generic `CELL_PARAMETERS` matrix that can carry tiny floating-point asymmetries showing up as force noise near a symmetric stationary point - `cell_type: cubic_supercell` should have mapped to `ibrav=1`/`celldm(1)` from the start. Fixed both: `ibrav=1` everywhere, plus `trust_radius_min` lowered to `1.0e-6` bohr (new `ion_trust_radius_min_bohr` config key) so BFGS doesn't abort prematurely. Applied to all 4 systems in `scripts/06_build_qe_inputs.py`, re-verified with smoke tests (no parse errors, same symmetry/RAM as before), `tests/test_qe_input_builder.py` now asserts the new content. 28/28 tests still pass.

## Known limitations

- `Ni` and `Cr` pseudopotentials (`ni_pbe_v1.4.uspp.F.UPF`, `cr_pbe_v1.5.uspp.F.UPF`) don't match the `_psl.` SSSP-efficiency naming convention used by Fe/C/H/O in the same directory — flagged in `configs/pseudo_manifest_required.yaml` as `naming_convention_warnings`, needs manual confirmation they're the correct SSSP-efficiency pseudopotentials before any production run.
- Reference values are all placeholders (`needs_manual_review`) — no literature/database values have been collected yet (script 03 deliberately performs no fetching, per project rules).
- Scripts 07–15 (parser, dataset validation, ML, uncertainty, active learning, report, nebwalk demo) are not yet built — they depend on real QE relax outputs that don't exist yet.
- Ferrocene relax has only been smoke-tested (~90s, a few SCF iterations) with the corrected padding, not run to completion. ni_co4/cr_co6/fe_co5 have not been re-tested with the new padding yet, though they all use the same QE settings and similarly modest molecular extents.

- Verified the padding fix holds for all 4 primary systems individually (short, bounded smoke tests, run sequentially while Rifat's real ferrocene relax was active so as not to starve it): ferrocene 10.50 GB/process, ni_co4 8.29 GB, cr_co6 13.81 GB, fe_co5 13.03 GB - all under the 16 GB WSL ceiling, all pseudopotentials load cleanly. cr_co6/fe_co5 run higher because their M-C-O arm length is longer at the same 6 A padding (bigger box, bigger FFT grid) - not a bug, but means these 4 relax jobs should be run sequentially, not concurrently, since e.g. cr_co6 alone is already close to the ceiling.

## Next action

Rifat reruns all 4 with the regenerated inputs (ibrav=1 + trust_radius_min fix) - ni_co4/fe_co5 should reproduce their converged results, cr_co6 should now actually converge. Once outputs land in `qe/outputs/relax/`, build script 07 (QE output parser) against real data before touching ML/AL scaffolding — per [[project-actistruct-kulik-plan]] flag, don't build ML/uncertainty/acquisition code ahead of having enough labeled rows.
