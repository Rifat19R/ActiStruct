# Assets

- `logo/actistruct-wordmark.svg` is the project wordmark.
- `figures/workflow.svg` is a conceptual software workflow, not a scientific
  result or benchmark plot.

Scientific figures must be generated from committed data by a reproducible
script and must identify that script and data source here. Decorative visuals
must never be presented as benchmark evidence.
